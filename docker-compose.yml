services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3003:3000"
    volumes:
      - ./vault:/app/vault
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
